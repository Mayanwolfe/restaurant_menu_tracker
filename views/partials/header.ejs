<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.7.2/css/all.css" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script>
        function showAddNewMenu() {
            let newMenu = document.getElementById('new-menu');
            newMenu.style.display = 'block';
        }
    </script>
  </head>
  <body>
    <div class="container d-flex justify-content-center">
        <div class="card mt-5 p-4">

            <div class="header-img">
                <div class="menu-icon">
                    <div class="dropdown">
                        <a href="#" class="dropbtn"><span class = 'fa fa-bars'></span></a>
                        <div class="dropdown-content">
                            <ul>
                                <% if (!loggedIn) { %>
                                    <li><a href="/login">Login</a></li>
                                <% } else { %>
                                    <li><a href="#" onclick="showAddNewMenu();return false;">Add new menu</a></li>
                                    <li><a href="/login/logout">Logout</a></li>
                                <% } %>
                            <ul>
                        </div>
                    </div>
                </div>
                <div class="app-icon"><a href="/"><img src="/images/menuhub_logo.png"></a></div>
            </div>
            
            <% if (loggedIn) { %>
            <div class="login-box">
                <div id="new-menu">
                    <h2 class="page-title">New Menu</h2>
                    <form action="/menu/new" method="POST">
                        <div class="menu-name"><input type="text" name="menuName" placeholder="Name"></div>
                        <div class="menu-icon-link"><input type="text" name="menuIcon" placeholder="Icon URL [http]"></div>
                        <div class="menu-url"><input type="text" name="menuUrl" placeholder="Menu URL [http]"></div>
                        <div class="menu-btn"><button type="submit" class="btn btn-primary">Save</button></div>
                    </form>
                </div>
            </div>
            <% } %>